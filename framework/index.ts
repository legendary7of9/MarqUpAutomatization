import { LoginPage } from './pages';
import { URL } from './url';

const pageProvider = (page:any) => {
    return {
        LoginPage
    }
}

const url = {
        URL
}

export {
    LoginPage,
    URL
}